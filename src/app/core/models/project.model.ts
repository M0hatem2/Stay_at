export interface Project {
  id: string;
  name: string;
  developer: string;
  location: string;
  imageUrl: string;
  deliveryDate: string;
  unitTypes: string[];
  startingPrice: number;
  totalUnits: number;
}

// Mock data for development

